<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.marketwatcher.android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name="fr.marketwatcher.android.MainActivity"
            android:label="@string/app_name" />
        <activity
            android:name="fr.marketwatcher.android.LoginActivity"
            android:theme="@style/LoginTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="fr.marketwatcher.android.CatalogActivity" />
        <activity android:name="fr.marketwatcher.android.ArticleActivity" />
        <activity android:name=".BarcodeResultActivity"
            android:label="Barcode Reader">
        </activity>
        <activity android:name=".ScanBarcodeActivity"
            android:label="Scan Barcode"></activity>
        <meta-data
            android:name="com.google.android.gms.vision.DEPENDENCIES"
            android:value="barcode" />
    </application>

</manifest>